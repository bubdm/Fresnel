
function initialise() {
    setupLayout();
}

function setupLayout() {

}

function setupCollectionGrid(collectionVM, gridElementName) {

    //$(gridElementName).w2grid({
    //    name: gridElementName,
    //    method: 'GET', // need this to avoid 412 error on Safari
    //    columns: []
    //});

    //var grid = $(gridElementName).w2grid;

    //collectionVM.ColumnHeaders.forEach(function (col) {
    //    grid.addColumn({ field: col, caption: col, size: '15%', sortable: true });
    //});

}